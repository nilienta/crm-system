import React from 'react';
import Title from '../title/title';

const title = 'React with Webpack and Babel';

export const App = () => {
  return (
    <div>
      <Title text={title} />
    </div>
  );
};
